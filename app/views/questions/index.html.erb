<!-- Everything from the index is extracted to a partial so that the same
	   view can be used DRY-ly in tags/show, with different values for
     @questions and header_text -->

<%= render "questions/index_corridor", header_text: "Top Questions" %>